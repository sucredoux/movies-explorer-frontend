import "./MoreButton.css";

function MoreButton() {
    return (
        <section className="more">
        <button
        type="button"
        aria-label="Ещё"
        className="more__button button"
        >Ещё</button>

        </section>
    );
};

export default MoreButton;